<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : t("anonymous_authorization.engine") %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "easy_style/common", media: "all" %>
    <%= javascript_importmap_tags "easy_style/application" %>
    <%= javascript_import_module_tag "easy_style/turbo" %>

    <%= yield(:head_additions) if content_for?(:head_additions) %>
  </head>
  <body>
    <main class="d-flex flex-direction-column h-100">
      <%= content_for?(:custom_flash_messages) ? yield(:custom_flash_messages) : render("easy_style/mobile/shared/flash_messages") %>

      <%= yield %>

      <%= content_for?(:custom_footer) ? yield(:custom_footer) : render("easy_style/shared/footer") %>
    </main>
  </body>
</html>
